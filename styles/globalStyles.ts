import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  backgroundColors: {
    backgroundColor: "#f0",
  },

  primarColor: {
    color: "#654321"
  },

  secondaryColor: {
    color: "#d4a373"
  },

  primaryText: {
    color: "#33"
  },

  secondaryText: {
    color: "white"
  }
})
