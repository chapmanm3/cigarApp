export type CigarResponse = {
  id: number,
  name: string,
  brand?: string,
  description?: string,
  image?: string,
  humidorId?: number,
  userId?: number,
}

export type Cigar = {
  id: number,
  name: string,
  brand?: string,
  description?: string,
  image?: string,
  humidorId?: number,
}
